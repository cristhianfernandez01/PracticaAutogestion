{% extends '@Afiliado/layout.html.twig' %}

{% block contenido %}

    <h1 class="page-header">
        Cambio de contrase&ntilde;a
    </h1>


    <div class="alert alert-success">
        <p>
            Para cambiar su contrase&ntilde;a debe ingresar primeramente, como medida de seguridad, su contrase&ntilde; actual.
            Tambien debe ingresar dos veces su contrase&ntilde;a nueva, para evitar posibles errores en el tipeo de la misma.
        </p>
    </div>

    <div class="col-lg-6">
        {{ form_start(form) }}
        {{ form_errors(form) }}

        <div class="form-group">
            {{ form_label(form.oldpassword, 'Contraseña actual:') }}
            {{ form_errors(form.oldpassword) }}
            {{ form_widget(form.oldpassword, {'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_label(form.newpassword, 'Nueva contraseña:') }}
            {{ form_errors(form.newpassword) }}
            {{ form_widget(form.newpassword, {'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_label(form.newpassword2, 'Escriba nuevamente la nueva contraseña:') }}
            {{ form_errors(form.newpassword2) }}
            {{ form_widget(form.newpassword2, {'attr': {'class': 'form-control '} }) }}
        </div>
        <ul class="list-group list-unstyled list-inline">
            <li>
                <div class="form-group">
                    {{ form_widget(form.aceptar, {'attr': {'class': 'btn btn-default'} } ) }}

                </div>
            </li>
            <li>
                <div class="form-group">
                    <a href="{{ path('datos_afiliado') }}" class="btn btn-default">
                        Cancelar
                    </a>

                </div>
            </li>
        </ul>

        {{ form_rest(form) }}
        {{ form_end(form) }}


    </div>



{% endblock %}