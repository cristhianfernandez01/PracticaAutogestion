{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tarjeta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numerotarjeta</th>
                <th>Descripciontarjeta</th>
                <th>Vencimiento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tarjeta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.numeroTarjeta }}</td>
                <td>{{ entity.descripcionTarjeta }}</td>
                <td>{{ entity.vencimiento }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tarjeta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tarjeta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tarjeta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
