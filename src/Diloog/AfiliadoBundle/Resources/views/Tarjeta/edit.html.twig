{% extends '@Afiliado/layout.html.twig' %}

{% block contenido %}
    <h1>Editar datos de Tarjeta</h1>

    {{ form(edit_form) }}

        <ul class="list-group list-unstyled list-inline">
    <li>
        <a href="{{ path('tarjeta') }}">
            Volver al listado
        </a>
    </li>
    <li>
        {{ form(delete_form) }}
    </li>
</ul>
{% endblock %}
